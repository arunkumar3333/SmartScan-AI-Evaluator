server:
  port: 8081

spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5433/smartscan_db
    username: postgres
    password: Deeksha@112

    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
