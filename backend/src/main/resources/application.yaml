server:
  port: 8081 # keep your fix

spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/smartscan_db # use correct port
    username: postgres
    password: Deeksha@112 # your actual password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
